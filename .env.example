# Copy this file to .env and fill in your values, then: source .env
# NEVER commit .env to git.

# Gmail address and App Password
# Generate an App Password at: https://myaccount.google.com/apppasswords
# (Requires 2-Step Verification to be enabled on your Google account)
EMAIL_FROM=your@gmail.com
EMAIL_PASSWORD=xxxx-xxxx-xxxx-xxxx

# Display name shown in recipients' inboxes
FROM_NAME=ClearJet Pressure Washing

# SMTP server (default is Gmail — change for Outlook, SendGrid, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Physical mailing address (required by CAN-SPAM)
PHYSICAL_ADDRESS=ClearJet Pressure Washing LLC, Memphis, TN 38117

# Unsubscribe destination — replace with a real URL once you have a domain
UNSUBSCRIBE_URL=mailto:unsubscribe@clearjetpw.com?subject=unsubscribe

# Max initial emails to send per run (ramp up slowly: 25 → 50 → 100)
DAILY_LIMIT=25
